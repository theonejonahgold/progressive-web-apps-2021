{{#unless this.deleted}}
{{#unless this.dead}}
<li>
  <article>
    <p>
      {{{this.text}}}
    </p>
    {{#if this.comments}}
    <ul>
      {{#each this.comments}}
      {{> partials/comment}}
      {{/each}}
    </ul>
    {{/if}}
  </article>
</li>
{{/unless}}
{{/unless}}